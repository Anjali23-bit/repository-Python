def count(str1):
    lower=0
    upper=0
    for i in str1:
        if(i.islower()):
            lower+=1
        elif(i.isupper()):
            upper+=1   
        else:
            print(" ")
    return lower,upper
str1=input(" ")    
print(count(str1))